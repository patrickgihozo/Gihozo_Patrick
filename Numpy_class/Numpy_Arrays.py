#A NumPy array is a collection of elements (usually numbers) stored together in a way that makes calculations fast and efficient.

#It is as a better version of a Python list for numerical data.
#Creating a numpy Array

#one dimensional Array from a list.
import numpy as np
array1 = np.array([10,20,30,40])
print(array1[1])

#Two dimensional Array from nested list

array2 =np.array([[10,30,50],[20,70,60]])
print(array2[0,2])